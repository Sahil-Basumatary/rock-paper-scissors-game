spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rpsarena
    username: your_username
    password: your_password
  data:
    redis:
      host: localhost
      port: 6379
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://your-app.clerk.accounts.dev

cors:
  allowed-origins: http://localhost:3000

